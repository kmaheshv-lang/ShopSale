version: 2

models:
  - name: Stg_COUNTRIES
    columns:
      - name: COUNTRY_ID
        tests:
          - not_null
          - unique
      - name: COUNTRY_NAME
        tests:
          - not_null
          - is_valid_length
          
          
